// dark-mode.ts

import { atom } from 'recoil'

export const darkModeAtom = atom({
    //

    // atom name
    key: 'app-dark-mode',

    // default value
    default: null as boolean | null

    //
})

// EOF
